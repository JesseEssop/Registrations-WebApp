<div class="RegNumbers">
    <div class="box1">
        <h1 class="center" style="margin-top:0px">Registration Numbers</h1>
    </div>

    <div class="row">
        <div class="col-12">
            <form action="/settings" method="POST">
                <div class="center" style="width:200px;">
                   <div style="color:white"> Enter Registration Number:</div>
                    <input class="textArea" type="string" maxlength=10 name="regnum">
                    <button class="button-primary addBtn" on type="submit" name="add">Add</button>
                    <button class="button-primary resetBtn" on value='reset' type="sumbit" name="reset">Reset</button>
                </div>
            </form>

            <form action="/filter" method="POST">
                <div class="center" style="width:200px;margin-bottom:10px;margin-top:20px;color: whitesmoke">
                    eg. CA 768 987
                    <select id="townDrop" name="townDropDown">
                        <option value="">Show All</option>
                        <option value="CA">Cape Town</option>
                        <option value="CY">Bellville</option>
                        <option value="CK">Malmesbury</option>
                    </select>
                    <button class="button-primary showBtn" type="submit" name="showReg">Show
                        Registration</button>
            </form>
        </div>


        <div class="center">
            <div class="Error">
                {{messages.error}}
            </div>

             <div class="Error2">
                {{messages.error2}}
            </div>
        </div>

        <div class="center">
            <strong>Registration Numbers:</strong>
            <ul>
                {{#each regNums}}
                <li>{{regnumber}}</li>
                {{/each}}
            </ul>
        </div>

    </div>
</div>
</div>